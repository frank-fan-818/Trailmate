# Trailmate 项目 AI 操作手册
## 文档说明
本文档为Trailmate智能旅行应用的AI操作参考手册，是所有AI工具参与项目开发的唯一权威参考，必须严格遵循本文档的所有规范和约束。

---

## 1. 项目概述
### 项目定位
Trailmate（伴旅）是一款智能旅行应用，系统性解决旅行者三大核心痛点：
- 行程规划耗时耗力
- 旅途不确定性困扰
- 结伴出行难以找到合适伙伴

### 架构模式
采用**微内核+插件化**分层架构：
```
前端展示层 → 微内核Core层 → 业务插件层 → 适配插件层
```
- 微内核Core层：100%稳定，无业务逻辑，提供事件总线、状态管理、插件管理、依赖注入、配置中心、安全拦截六大核心能力
- 业务插件层：每个功能模块为独立插件，包括行程规划、情境感知、社交匹配、基础服务四大模块
- 适配插件层：对接第三方服务，包括AI大模型、地图服务、OTA服务、Mock数据适配

### 核心技术要求
- 优先使用TypeScript语言开发，严格遵循类型安全规范
- 核心层与业务层完全解耦，模块间仅通过事件总线通信，禁止直接依赖
- 所有功能扩展必须通过插件方式实现，禁止修改核心层代码
- 优先保障交互流程完整性，演示版本后端逻辑和第三方接口暂时使用Mock实现，但如果用户没有让你生成测试数据，也不能依赖Mock数据。如果用户给你提供真实接口，你就不能使用Mock数据，并应当向用户询问是否应该将模拟数据删除。

---

## 2. 技术栈规范
| 技术/框架 | 版本要求 | 用途 | 优先级 |
|----------|---------|------|-------|
| Vue | ^3.4.0 | 前端框架 | 必选 |
| TypeScript | ^5.3.0 | 类型安全开发语言 | 必选 |
| Vite | ^5.0.0 | 前端构建工具 | 必选 |
| Vitest | ^4.1.0 | 单元测试框架 | 必选 |
| Tailwind CSS | ^3.4.0 | 原子化CSS框架 | 必选 |
| Node.js | >=18.0.0 | 运行环境 | 必选 |
| npm | >=9.0.0 | 包管理工具 | 必选 |

### 开发工具规范
- IDE：优先使用VS Code，安装Volar、ESLint、Prettier插件
- 代码格式化：统一使用Prettier配置，禁止手动调整格式化规则
- 版本控制：使用Git，提交信息遵循团队自定义规范

---

## 3. 文件组织和命名规范
### 目录结构规范
```
Trailmate/
├── core/                     # 微内核核心层（一般禁止修改，除非用户明确要求）
│   ├── interfaces/           # 全局标准接口定义
│   ├── event-bus.ts          # 事件总线
│   ├── state-manager.ts      # 状态管理器
│   ├── plugin-manager.ts     # 插件管理器
│   ├── di-container.ts       # 依赖注入容器
│   ├── config-center.ts      # 配置中心
│   └── security-interceptor.ts # 安全拦截器
├── modules/                  # 业务功能插件
│   ├── base/                 # 基础服务模块
│   ├── trip-tools/           # 行程工具类模块
│   ├── perception/           # 情境感知模块
│   └── social/               # 社交功能模块
├── adapters/                 # 第三方服务适配层
│   └── mock-adapter/         # 模拟数据适配器
├── platforms/                # 多端实现层
│   └── web/                  # Web演示端
├── shared/                   # 公共工具库（无状态）
├── CLAUDE.md                 # AI操作手册（本文档）
├── package.json
├── tsconfig.json
├── .gitignore
└── README.md
```

### 命名规范
1. **目录命名**：统一使用小写 kebab-case 格式，如 `trip-tools`、`itinerary-generator`
2. **文件命名**：TypeScript文件使用小写 kebab-case 格式，如 `event-bus.ts`、`state-manager.ts`
3. **类命名**：使用大驼峰 PascalCase 格式，如 `EventBus`、`StateManager`
4. **函数/变量命名**：使用小驼峰 camelCase 格式，如 `generatePlans`、`currentUser`
5. **常量命名**：使用全大写下划线格式，如 `MAX_RETRY_TIMES`、`DEFAULT_DATE_FORMAT`
6. **接口命名**：使用大驼峰 PascalCase 格式，前缀加 `I`，如 `IPlugin`、`ICore`

---

## 4. 组件架构模式
### 插件开发规范
所有业务功能必须实现`IPlugin`接口，包含以下生命周期方法：
```typescript
interface IPlugin {
  pluginId: string;       // 插件唯一标识，格式：模块名-功能名
  pluginName: string;     // 插件中文名称
  version: string;        // 版本号
  dependencies?: string[];// 依赖的其他插件ID列表
  
  onInstall(core: ICore): Promise<void>;  // 安装时执行
  onMount(core: ICore): Promise<void>;    // 挂载时执行
  onUnmount(core: ICore): Promise<void>;  // 卸载时执行
}
```

### 状态管理规范
- 全局状态统一使用`StateManager`进行管理，禁止组件内部维护全局状态
- 状态变更必须通过`state.set()`方法触发，禁止直接修改状态对象
- 跨模块状态同步必须通过订阅状态变更事件实现，禁止直接读取其他模块状态

### 组件通信规范
- 模块间通信统一使用`EventBus`事件总线，禁止直接调用其他模块的方法
- 事件命名遵循`领域.操作.状态`格式，如`plan.request`、`plan.generated`
- 所有事件必须在`GlobalEvent`枚举中定义，禁止使用字符串字面量作为事件名

---

## 5. 设计系统实现
### 设计Token规范
设计Token统一分为以下类别：
1. **颜色Token**：`--color-primary`、`--color-success`、`--color-warning`、`--color-danger`等
2. **间距Token**：`--spacing-xs`、`--spacing-sm`、`--spacing-md`、`--spacing-lg`等
3. **字体Token**：`--font-size-xs`到`--font-size-xxl`，`--font-weight-normal`、`--font-weight-bold`等
4. **圆角Token**：`--radius-sm`、`--radius-md`、`--radius-lg`、`--radius-full`等
5. **阴影Token**：`--shadow-sm`、`--shadow-md`、`--shadow-lg`等

### 样式开发规范
- 优先使用Tailwind CSS原子类实现样式，禁止硬编码颜色、间距等数值
- 组件样式使用 scoped 隔离，避免全局样式污染
- 响应式设计统一使用Tailwind默认断点：`sm: 640px`、`md: 768px`、`lg: 1024px`、`xl: 1280px`

### UI组件规范
- 优先使用Tailwind CSS实现UI组件，保持样式一致性
- 通用组件必须放在`shared/components`目录下，业务组件放在对应模块目录下
- 组件必须提供完整的TypeScript类型定义，禁止使用`any`类型

---

## 6. 操作规则和约束
### 允许操作
1. ✅ 新增业务插件模块，扩展项目功能
2. ✅ 修改业务插件内部代码，修复bug或新增功能
3. ✅ 新增适配层插件，对接第三方服务
4. ✅ 修改平台层（Web/移动端）代码，优化用户界面
5. ✅ 新增共享工具函数，完善公共能力

### 禁止操作（如果用户明确让你操作，要向用户再次确认）
1. ❌ 禁止修改core核心层的任何代码，核心层功能仅可通过插件扩展
2. ❌ 禁止破坏微内核架构，禁止模块间直接依赖，所有通信必须通过事件总线
3. ❌ 禁止提交`node_modules`、`dist`、日志文件、编辑器配置文件到版本库
4. ❌ 禁止在代码中硬编码敏感信息（API密钥、密码等）
5. ❌ 禁止修改`.gitignore`、`tsconfig.json`等全局配置文件，如需修改必须先确认
6. ❌ 禁止使用不符合技术栈规范的第三方库，新增依赖必须先评估
7. ❌ 禁止在未明确用户需求的情况下自行决定实现方案，所有不确定内容必须询问用户

### 必须遵守的强制规则
1. 每次对文件夹中的任何内容进行修改或变动后，必须更新对应的`README.md`文件，说明当前版本的进度和变更内容
2. 编写代码时，但凡遇到任何不确定的内容（如使用什么接口、开发环境、版本号等），必须至少向用户追问一次确认
3. 一次性编写代码不要过多，如果要同时修改多个文件，必须向用户通知并解释原因，等待用户指示后再操作
4. 单元测试覆盖率必须达到80%以上，核心模块必须100%覆盖测试
5. 当你认为和用户的对话轮次已经够多之后，或者你已经连续修改多次（最多不超过5次）文件代码，请你要主动向用户发起询问：要不要将代码全部审查一遍，确认用户是否同意，然后对已编写的代码进行审查，但不要直接修改，而是要用户确认后再修改。

### 边界条件
- 演示版仅支持最多50个注册用户，10人并行操作
- 所有涉及用户隐私的操作必须经过安全拦截器校验
- 第三方服务调用失败时必须自动降级到Mock数据，保证系统可用性

---

## 7. 部署规范
### 构建流程
1. 安装依赖：`npm install`
2. 类型检查：`npm run typecheck`
3. 单元测试：`npm run test`
4. 生产构建：`npm run build:web`
5. 构建产物输出到`dist`目录

### 发布流程
1. 测试环境：自动部署代码到测试服务器，运行集成测试
2. 预发布环境：灰度发布给部分用户，验证功能稳定性
3. 生产环境：全量发布，监控系统运行状态

### 回滚机制
- 每个版本保留构建产物，出现问题时可快速回滚到上一个稳定版本
- 数据库变更必须包含回滚脚本，确保数据安全性

---

## 8. 上下文信息
### 项目开发规划（三个月）
| 阶段 | 时间 | 核心目标 | 完成状态 |
|------|------|----------|---------|
| 第一月 | 第1-4周 | 完成智能规划引擎模块开发，支持自然语言输入生成多套行程方案 | ✅ 已完成 |
| 第二月 | 第5-8周 | 完成情境感知服务模块开发，实现行程时间线与主动提示功能 | ⏳ 进行中 |
| 第三月 | 第9-12周 | 完成社交匹配系统模块开发，实现旅伴匹配、临时组队、信用评价功能 | 📅 未开始 |

### 业务需求优先级
1. P0：核心流程完整性，保证用户可以完整使用行程规划功能
2. P1：用户体验优化，界面美观度、交互流畅度优化
3. P2：功能扩展，新增情境感知、社交匹配等高级功能
4. P3：性能优化，系统吞吐量、响应速度优化

### 领域特定知识
- 行程方案默认提供两套：亲子休闲版（节奏宽松、儿童友好）、探索打卡版（景点密集、性价比高）
- 情境感知功能需要根据用户位置、时间、天气等信息主动推送提示
- 社交匹配功能需要基于用户旅行偏好、出行时间、目的地等维度进行智能匹配
- 所有用户生成内容必须经过内容安全审核，敏感信息自动过滤
